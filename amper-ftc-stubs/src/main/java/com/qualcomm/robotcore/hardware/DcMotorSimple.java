package com.qualcomm.robotcore.hardware;

/** Compile-only stub matching FTC {@code DcMotorSimple}. */
public interface DcMotorSimple extends HardwareDevice {
    void setPower(double power);

    double getPower();
}
